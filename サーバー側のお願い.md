# サーバー側（Google Apps Script）に足していただくこと

ここに書いたものは、**画面の側（この場所のファイル）だけでは、できません**。
Google Apps Script（GAS）に手を入れていただく必要があります。

画面の側は、もう出来ています。GAS がそろえば、そのまま動きます。

新規登録のときに、GAS へは次のものが届いています。

```
parts : ['guide','room','terms'] のうち、印を付けたものだけ
        guide … 入居のしおり（駐車場・ポスト・ゴミ・暮らしのルール）
        room  … 室内チェック
        terms … 重要事項（特約の確認）
```

あわせて、特約の一覧（`clauses`）のいちばん後ろにも、
`【送付内容】しおり・室内・重要事項` のような1行が入っています。
`parts` を保存しにくいときは、この1行を見ていただいても構いません。

---

## 1【いそぎ】ご案内メールの文面を、送った内容に合わせる

いまは、何を送っても**同じ文面**が届いています。
入居のしおりだけをお送りした方にも「お部屋の中に、入居前からのキズ・汚れ・
不具合が無いかご確認のうえ……」と書かれてしまいます。

`parts` を見て、書き分けてください。

| 送った内容 | ご案内の本文（ねらい） |
| --- | --- |
| `room` を含む | いまの文のまま（お部屋の確認のお願い） |
| `terms` を含む | 「ご契約時にご説明のあった内容のご確認」のお願い |
| `guide` だけ | **確認のお願いは書かない**。「入居のしおり（駐車場の区画・集合ポストのダイヤル・ゴミの出し方・暮らしのルール）をご覧ください」だけにする |

`guide` だけのときの文例

```
このたびは ○○ ○○号室 にご入居いただき、ありがとうございます。
IREライフ株式会社 です。

入居のしおりを、下記のページにご用意しております。
駐車場の区画・集合ポストのダイヤル・ゴミの出し方・暮らしのルールを
まとめておりますので、ご入居前にお読みください。
このページは、ご入居後もいつでもご覧いただけます。

▼ 入居のしおり
https://……/?id=……
パスワード： ……
```

※「■ ご返信の期限」「ご確認のうえお知らせください」は、`guide` だけのときは
書かないでください（ご返信をいただくものがないためです）。

---

## 2【いそぎ】入居のしおりだけの件は、督促しない・完了にする

ご返信をお待ちするものがありませんので、**お送りした時点で「完了」**です。

- 期限が来ても、**自動の督促メールを送らない**でください
- 作成のときに、その行を「完了」にしていただくのがいちばん確実です

画面の側では、すでに次のようにしています。

- しおりだけの件は「未返信」には出さず、「完了」に入れています
- その件を開いても「督促メールを送る」は出しません
- 作成のとき、GAS が `id` を返してくれた場合は、続けて
  `{action:'done', key, id, who}` を送り、あちらの記録も「完了」にしています
  （`id` を返していない場合は、画面の見た目だけが「完了」になります。
  **この場合、自動の督促は止まりませんので、GAS 側での対応が必要です**）

---

## 3 宛先を直して送り直す

管理画面で1件を開くと、**「宛先を直して送り直す」**のボタンがあります。
押して新しいアドレスを入れると、GAS へ次のものを送ります。

```
{ action : 'resend',
  key    : '管理用パスワード',
  id     : 'その件の id',
  mail   : '新しいメールアドレス',
  who    : '操作した人のお名前' }
```

### GAS でしていただくこと

- その `id` の行の、メールアドレスを `mail` に書き替える
- ご案内のメールを、その宛先へ**もう一度**送る
  （パスワードもリンクも、これまでと同じもので構いません。作り直す必要はありません）
- 下の「4」で付けた不達の印を、消す
- 操作の記録に「宛先を直して送り直した（`who` さん）」を残す

### 返していただくもの

| ばあい | 返すもの |
| --- | --- |
| 送れた | `{ ok:true }` |
| 送れなかった | `{ ok:false, err:'理由' }` |

`err` に入れた文は、そのまま画面に出ます。

---

## 4 メールが届かなかったこと（不達）の感知

### 送った時点で分かるもの

アドレスの形がおかしいとき（`@` が無い、など）は、その場で送信に失敗します。
これは、いまも「依頼はつくりましたが、メールを送れませんでした」と出ています。

### あとから分かるもの

形は正しいけれど、そのアドレスが**存在しない**とき（`gmail` → `gmial` など）は、
送った時点では分かりません。数十秒〜数分のあいだに、**送信元の Gmail へ
「Address not found」の不達通知**が届きます。これを見れば、分かります。

### GAS でしていただくこと（1時間ごとなどのトリガーで）

```javascript
// 送信元の Gmail から、不達のお知らせをさがします
var th = GmailApp.search('from:mailer-daemon newer_than:2d');
// 本文に入っているアドレスを取り出し、同じアドレスの行に印を付けます
```

印は、表に1列（たとえば「不達」）を足して、日付や理由を入れてください。
`list` でその行を返すときに、**次のどれかの名前**で入れていただければ、
管理画面は、そのカードに**赤字で「アドレス相違による未送信」**と出します。

| 名前 | 入れるもの |
| --- | --- |
| `bounce` / `mailNg` / `mailErr` | 日付や理由の文字（例：`2026/09/22 Address not found`） |
| `mailStatus` | 「未送信」「不達」などの言葉 |

入っていた文字は、カードと、その件の中の赤いお知らせに、そのまま出ます。

※ 送信元の Gmail を読みますので、GAS に **Gmail の許可**が必要です。
※ 「完了」になった件には、不達の印が残っていても出しません。
※ 不達の印が付いた件は、しおりだけの件であっても「未返信」に出します
　（送り直していただく必要があるためです）。

---

## 5 `list` と `detail` で、送った内容を返す

管理画面の「この件の情報」に、**送った内容**（入居のしおり・室内チェック・
重要事項）を出しています。次のどちらかが返ってくれば出ます。

- `parts`（`['guide','room']` のような並び）
- `clauses`（いちばん後ろの `【送付内容】…` の1行から読みます）

`list` で返していただくと、一覧の振り分け（しおりだけの件を「完了」に入れる）
にも使われます。**いまはここが要です**ので、できれば `list` でお願いいたします。
